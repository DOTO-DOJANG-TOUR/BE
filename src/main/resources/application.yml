spring:
  config:
    import: optional:file:/app/config/.env[.properties]
  application:
    name: doto
  profiles:
    default: local
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 20260806

server:
  shutdown: graceful

management:
  endpoints:
    web:
      base-path: /actuator
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
  api-docs:
    path: /v3/api-docs

doto:
  cache:
    maximum-size: ${CACHE_MAXIMUM_SIZE:1000}
    expire-after-write: ${CACHE_EXPIRE_AFTER_WRITE:10m}
    active-tour-spot-visit-expire-after-write: ${ACTIVE_TOUR_SPOT_VISIT_CACHE_EXPIRE_AFTER_WRITE:30s}

jwt:
  expiration-seconds: ${JWT_EXPIRATION_SECONDS:3600}
  refresh-expiration-seconds: ${JWT_REFRESH_EXPIRATION_SECONDS:1209600}

oidc:
  kakao:
    client-id: ${KAKAO_OIDC_CLIENT_ID:}
    issuer: https://kauth.kakao.com
    jwk-set-uri: https://kauth.kakao.com/.well-known/jwks.json
  google:
    client-id: ${GOOGLE_OIDC_CLIENT_ID:}
    issuer: https://accounts.google.com
    jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs

tour-api:
  base-url: ${TOUR_API_BASE_URL:https://apis.data.go.kr/B551011/KorService2}
  service-key: ${TOUR_API_SERVICE_KEY:}
  connect-timeout: ${TOUR_API_CONNECT_TIMEOUT:3s}
  read-timeout: ${TOUR_API_READ_TIMEOUT:5s}
  sync:
    enabled: ${TOUR_API_SYNC_ENABLED:false}
